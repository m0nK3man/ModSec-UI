image:
  name: alpine/alpine

stages:
  - deploy

deploy:
  stage: deploy
  variables:
    GIT_STRATEGY: clone
  when: always # skip this stage on auto run
  only:
    - main
  script:
    - echo "hello world"
    - pwd
    - whoami
    - ls -la